name: Hotel Pricing Bot
# Workflow Version: v5
# Last Updated: 2026-07-17 18:50 UTC
# Cache Buster: timestamp-18-50

env:
  WORKFLOW_VERSION: "v5-npm-start-npm-build-cache-cleaned"

on:
  schedule:
    # Ejecutar cada 5 minutos
    - cron: '*/5 * * * *'

  # Permitir ejecución manual
  workflow_dispatch:

jobs:
  pricing-cycle:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Clonar repositorio (para leer VERSION)
        uses: actions/checkout@v4

      - name: 🔖 Mostrar versión del workflow (VERIFICAR QUE CAMBIOS LLEGARON)
        run: |
          VERSION=$(cat VERSION)
          echo ""
          echo "╔════════════════════════════════════════════════════════════╗"
          echo "║                   🔖 VERSIÓN DEL WORKFLOW                  ║"
          echo "╠════════════════════════════════════════════════════════════╣"
          echo "║ VERSION: $VERSION"
          echo "║ Ejecutado: $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
          echo "║ Commit: ${{ github.sha }}"
          echo "║ Run ID: ${{ github.run_id }}"
          echo "╚════════════════════════════════════════════════════════════╝"
          echo ""
          echo "✅ SI VES ESTA VERSIÓN = Los cambios llegaron a GitHub"
          echo ""

      - name: 🔍 Validar GitHub Secrets
        run: |
          echo "Validando secrets necesarios..."
          if [ -z "${{ secrets.DATABASE_URL }}" ]; then echo "❌ DATABASE_URL no configurado"; exit 1; fi
          if [ -z "${{ secrets.PXSOL_API_KEY }}" ]; then echo "❌ PXSOL_API_KEY no configurado"; exit 1; fi
          if [ -z "${{ secrets.PXSOL_API_URL }}" ]; then echo "❌ PXSOL_API_URL no configurado"; exit 1; fi
          if [ -z "${{ secrets.HOTEL_ID }}" ]; then echo "❌ HOTEL_ID no configurado"; exit 1; fi
          echo "✅ Todos los secrets están configurados"

      - name: 🔧 Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: 📦 Instalar dependencias
        run: npm install

      - name: 🔧 Compilar TypeScript
        run: npm run build

      - name: 🔑 Crear archivo .env
        run: |
          cat > .env << 'EOF'
          DATABASE_URL=${{ secrets.DATABASE_URL }}
          PX_API_KEY=${{ secrets.PXSOL_API_KEY }}
          PX_BASE_URL=${{ secrets.PXSOL_API_URL }}
          PX_HOTEL_ID=${{ secrets.HOTEL_ID }}
          NODE_ENV=${{ secrets.NODE_ENV || 'production' }}
          LOG_LEVEL=${{ secrets.LOG_LEVEL || 'info' }}
          EOF

      - name: 📊 Ejecutar migraciones Prisma
        run: npm run prisma:generate && npx prisma migrate deploy

      - name: 🚀 Ejecutar ciclo de pricing
        run: npm start
        env:
          SINGLE_CYCLE: "true"
        timeout-minutes: 10

      - name: 📋 Guardar logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pricing-logs-${{ github.run_number }}
          path: logs/
          retention-days: 7

      - name: ✅ Notificar éxito
        if: success()
        run: echo "✅ Ciclo de pricing completado exitosamente"

      - name: ❌ Notificar error
        if: failure()
        run: |
          echo "❌ Error en ciclo de pricing"
          exit 1
